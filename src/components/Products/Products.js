import React from "react";
import Header from "../Header/Header";

const Products = () => {
    return (
        <div>
            <h2>This is the products Components</h2>
        </div>
    );
};

export default Products;
